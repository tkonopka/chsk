import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'

<Meta title="Introduction" />

# Chask

'chask' is a charting library for React.

The name `chask` stands for 'chart skeletons'.
The library provides components that can be used to compose data-based visualizations.

## Why `chask`?

There are several charting libraries for javascript and for React.
