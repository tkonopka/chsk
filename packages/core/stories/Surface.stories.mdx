import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { Chart, BackgroundSurface, Surface } from '../src'
import { ChartDecorator } from './decorators'

# Surface

<Meta title="Core/Components/Surface" />
export const Template = args => <Surface {...args} />

`Surface` displays a rectangle.

<ArgsTable of={Surface} />

## Examples

Surfaces can be tuned using css.

<Canvas>
    <Story
        name="fill color"
        args={{
            variant: 'custom',
            style: { fill: '#dd9999' },
            x: 30,
            y: 20,
            size: [160, 90],
        }}
        component={Surface}
        decorators={[ChartDecorator]}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="stroke color"
        args={{
            variant: 'custom',
            style: { fill: '#dd9999', stroke: '#662222', strokeWidth: '2px' },
            x: 50,
            y: -20,
            size: [160, 90],
        }}
        component={Surface}
        decorators={[ChartDecorator]}
    >
        {Template.bind({})}
    </Story>
</Canvas>
