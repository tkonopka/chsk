import { Meta, Source } from '@storybook/addon-docs'

<Meta title="Core/Hooks/useTooltip" />

# `useTooltip`

The `useTooltip` hook retrieves information pertaining to a tooltip.

<Source
    language="javascript"
    code={`
import { useTooltip } from '@chsk/core'

const { data, setData } = useTooltip()
`} />
