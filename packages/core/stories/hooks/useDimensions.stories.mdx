import { Meta, Source } from '@storybook/addon-docs'

<Meta title="Core/Hooks/useDimensions" />

# `useDimensions`

The `useDimensions` hook provides information about a chart 'view', i.e. the chart area intended for drawing.

<Source
    language="javascript"
    code={`
import { useScales } from '@chsk/core'

const dimensions = useDimensions()
`} />
