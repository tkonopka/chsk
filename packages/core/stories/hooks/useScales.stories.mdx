import { Meta, Source } from '@storybook/addon-docs'

<Meta title="Core/Hooks/useScales" />

# `useScales`

The `useScales` hook returns an object with scales for the x-axis, y-axis, color, and symbols size.

<Source
    language="javascript"
    code={`
import { useScales } from '@chask/core'

const scales = useScales()
`} />
