import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { Chart, Path } from '../../../src'
import { ChartDecorator } from '../decorators'

# Path

<Meta title="Core/Components/Shapes/Path" />
export const Template = args => <Path {...args} />

`Path` displays a (curved) line.

<ArgsTable of={Path} />

## Open curves

export const openCurve = [
    [0, 200],
    [80, 170],
    [160, 50],
    [240, 90],
    [320, 20],
]

Paths are defined by a series of points, and interpolations are controlled via the prop `curve`. The examples below show distinct curves produced by the same set of points.

<Canvas>
    <Story
        name="linear"
        args={{
            points: openCurve,
            curve: 'Linear',
            style: { stroke: '#993333' },
        }}
        component={Path}
        decorators={[ChartDecorator]}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="natural"
        args={{
            points: openCurve,
            curve: 'Natural',
            style: { stroke: '#993333' },
        }}
        component={Path}
        decorators={[ChartDecorator]}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="monotoneX"
        args={{
            points: openCurve,
            curve: 'MonotoneX',
            style: { stroke: '#993333' },
        }}
        component={Path}
        decorators={[ChartDecorator]}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="step"
        args={{
            points: openCurve,
            curve: 'Step',
            style: { stroke: '#993333' },
        }}
        component={Path}
        decorators={[ChartDecorator]}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## Closed curves

export const closedCurve = [
    [50, 120],
    [140, 50],
    [270, 90],
    [200, 190],
    [100, 100],
]

Paths can display closed curves.

<Canvas>
    <Story
        name="linear closed"
        args={{
            points: closedCurve,
            curve: 'LinearClosed',
            style: { stroke: '#993333' },
        }}
        component={Path}
        decorators={[ChartDecorator]}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="basis closed"
        args={{
            points: closedCurve,
            curve: 'BasisClosed',
            style: { stroke: '#993333' },
        }}
        component={Path}
        decorators={[ChartDecorator]}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="cardinal closed"
        args={{
            points: closedCurve,
            curve: 'CardinalClosed',
            style: { stroke: '#993333' },
        }}
        component={Path}
        decorators={[ChartDecorator]}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="catmullRom closed"
        args={{
            points: closedCurve,
            curve: 'CatmullRomClosed',
            style: { stroke: '#993333' },
        }}
        component={Path}
        decorators={[ChartDecorator]}
    >
        {Template.bind({})}
    </Story>
</Canvas>
