import { ArgTypes, Canvas, Meta } from '@storybook/blocks'
import { Toolbar } from '../../../src'

# Toolbar

<Meta title="Core/Components/Interactivity/Toolbar" />

`Toolbar` is a container for a group of buttons.

<ArgTypes of={Toolbar} />
