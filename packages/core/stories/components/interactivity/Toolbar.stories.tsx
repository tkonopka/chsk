import { Toolbar } from '../../../src'

export default {
    title: 'Core/Components/Interactivity/Toolbar',
    component: Toolbar,
}

export const Default = {
    render: () => <></>,
    name: 'default',
}
