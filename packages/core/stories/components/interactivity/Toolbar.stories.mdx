import { ArgsTable, Canvas, Meta, Source, Story } from '@storybook/addon-docs'
import { Chart, View, Toolbar } from '../../../src'

# Toolbar

<Meta title="Core/Components/Interactivity/Toolbar" />
export const Template = args => <Toolbar {...args} />

`Toolbar` is a container for a group of buttons.

<ArgsTable of={Toolbar} />
