import { ArgsTable, Canvas, Meta, Source, Story } from '@storybook/addon-docs'
import { Chart, Legend, LegendSizeScale } from '../../src'
import { ChartWithSizeLegendDecorator } from './decorators'

# LegendSizeScale

<Meta title="Core/Components/LegendSizeScale" />
export const Template = args => <LegendSizeScale {...args} />

`LegendSizeScale` draws a rectangle with a color scale.

<ArgsTable of={LegendSizeScale} />

## Variants

Prop `variant` determines where the labels marks appear in relation to symbol.

<Canvas>
    <Story
        name="bottom"
        args={{
            variant: 'bottom',
            horizontal: true,
            position: [0, 0],
            itemSize: [25, 12],
            ticks: [10, 20, 30],
            labelOffset: 16,
            labelStyle: { textAnchor: 'middle' },
            symbolStyle: { fill: '#888888' },
        }}
        component={LegendSizeScale}
        decorators={[ChartWithSizeLegendDecorator]}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="top"
        args={{
            variant: 'top',
            horizontal: true,
            position: [0, 20],
            itemSize: [25, 12],
            ticks: [10, 20, 30],
            labelOffset: 14,
            labelStyle: { textAnchor: 'middle' },
            symbolStyle: { fill: '#888888' },
        }}
        component={LegendSizeScale}
        decorators={[ChartWithSizeLegendDecorator]}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="left"
        args={{
            variant: 'left',
            horizontal: false,
            position: [50, 24],
            itemSize: [12, 25],
            ticks: [10, 20, 30],
            labelOffset: 20,
            symbolStyle: { fill: '#888888' },
        }}
        component={LegendSizeScale}
        decorators={[ChartWithSizeLegendDecorator]}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="right"
        args={{
            variant: 'right',
            horizontal: false,
            position: [0, 24],
            itemSize: [12, 25],
            ticks: [10, 20, 30],
            symbolStyle: { fill: '#888888' },
        }}
        component={LegendSizeScale}
        decorators={[ChartWithSizeLegendDecorator]}
    >
        {Template.bind({})}
    </Story>
</Canvas>
