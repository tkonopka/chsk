import { ArgsTable, Canvas, Meta, Source, Story } from '@storybook/addon-docs'
import { Chart, Legend, LegendItem, Square, Circle } from '../../../src'
import { ChartWithRightLegendDecorator } from './decorators'

# LegendItem

<Meta title="Core/Components/Legends/LegendItem" />
export const Template = args => <LegendItem {...args} />

`LegendItem` draws one entry in a legend.

<ArgsTable of={LegendItem} />

## Variants

`LegendItem` draws a symbol and a corresponding text label.
Prop `variant` determines how the legend symbol and label are arranged.
The terminology matches that of axis and tick labels.
For example, variant 'right' makes the text label appear to the right of the symbol
(analogously to how labels appear to the right of tick marks on an axis placed on the right edge of a chart).

<Canvas>
    <Story
        name="right"
        args={{
            variant: 'right',
            position: [0, 16],
            size: [80, 28],
            r: 10,
            symbolStyle: { fill: '#f0f0f0', stroke: '#222222', strokeWidth: 1 },
            label: 'right',
            labelOffset: 14,
            labelStyle: { textAnchor: 'start' },
        }}
        component={LegendItem}
        decorators={[ChartWithRightLegendDecorator]}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="left"
        args={{
            variant: 'left',
            position: [0, 16],
            size: [80, 42],
            r: 9,
            symbolStyle: { fill: '#f0f0f0', stroke: '#222222', strokeWidth: 1 },
            label: 'left',
            labelOffset: 14,
            labelStyle: { textAnchor: 'end' },
        }}
        component={LegendItem}
        decorators={[ChartWithRightLegendDecorator]}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="bottom"
        args={{
            variant: 'bottom',
            position: [0, 16],
            size: [80, 42],
            r: 9,
            symbolStyle: { fill: '#f0f0f0', stroke: '#222222', strokeWidth: 1 },
            label: 'bottom',
            labelOffset: 18,
            labelStyle: { textAnchor: 'middle' },
        }}
        component={LegendItem}
        decorators={[ChartWithRightLegendDecorator]}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="top"
        args={{
            variant: 'top',
            position: [0, 16],
            size: [80, 42],
            r: 9,
            symbolStyle: { fill: '#f0f0f0', stroke: '#222222', strokeWidth: 1 },
            label: 'top',
            labelOffset: 18,
            labelStyle: { textAnchor: 'middle' },
        }}
        component={LegendItem}
        decorators={[ChartWithRightLegendDecorator]}
    >
        {Template.bind({})}
    </Story>
</Canvas>

Note that `variant` is not the only prop that affects placement of the text label. It is often necessary to adjust the css style for the label via prop `labelStyle`.

## Symbols

Use prop `symbol` to change the symbol.

<Canvas>
    <Story
        name="circle"
        args={{
            variant: 'right',
            position: [0, 16],
            size: [80, 28],
            r: 9,
            symbol: Circle,
            symbolStyle: { fill: '#dd3333', strokeWidth: 0 },
            label: 'label',
            labelOffset: 16,
        }}
        component={LegendItem}
        decorators={[ChartWithRightLegendDecorator]}
    >
        {Template.bind({})}
    </Story>
</Canvas>
