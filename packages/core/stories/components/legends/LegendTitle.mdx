import { ArgTypes, Canvas, Meta } from '@storybook/blocks'
import * as LegendTitleStories from './LegendTitle.stories'

# LegendTitle

<Meta of={LegendTitleStories} />

`LegendTitle` draws a text component intended as a legend title.

<ArgTypes of={LegendTitleStories} />

## Position

Positioning of `LegendTitle` text works in the same way as for ordinary `Typography` components.

<Canvas of={LegendTitleStories.Title} />
