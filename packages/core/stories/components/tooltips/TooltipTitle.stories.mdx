import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { TooltipTitle, Typography } from '../../../src'
import { ChartWithDetectorWithTooltipDecorator } from './decorators'

# TooltipTitle

<Meta title="Core/Components/Tooltips/TooltipTitle" />
export const Template = args => <TooltipTitle {...args} />

`TooltipTitle` draws a text component intended as a tooltip title.

<ArgsTable of={TooltipTitle} />

## Position

Positioning of `TooltipTitle` text works in the same way as for ordinary `Typography` components,
but the coordinates are measured from the top-left corner or the tooltip container.

<Canvas>
    <Story
        name="tooltip title"
        args={{
            position: [0, 12],
            size: [100, 28],
            children: 'Tooltip title',
        }}
        component={TooltipTitle}
        decorators={[ChartWithDetectorWithTooltipDecorator]}
    >
        {Template.bind({})}
    </Story>
</Canvas>
