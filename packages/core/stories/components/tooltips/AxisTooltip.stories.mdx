import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { AxisTooltip, Surface, Circle } from '../../../src'
import { ChartForDetectorTooltipDecorator, ChartForShapesTooltipDecorator } from './decorators'

# AxisTooltip

<Meta title="Core/Components/Tooltips/AxisTooltip" />
export const Template = args => <AxisTooltip {...args} />

`AxisTooltip` is a container component for tooltips.

<ArgsTable of={AxisTooltip} />

`AxisTooltip` is similar to `Tooltip` in that it displays information intermittently
using the `useTooltip` hook.

## Variant

Prop `variant` displays a tooltip box at one of the four edges of a view.

<Canvas>
    <Story
        name="top"
        args={{
            variant: 'top',
            size: [20, 20],
            padding: [0, 0, 0, 0],
            children: <Surface style={{ fill: '#aabbee' }} />,
        }}
        component={AxisTooltip}
        decorators={[ChartForDetectorTooltipDecorator]}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="right"
        args={{
            variant: 'right',
            size: [20, 20],
            padding: [0, 0, 0, 0],
            children: <Surface style={{ fill: '#aabbee' }} />,
        }}
        component={AxisTooltip}
        decorators={[ChartForDetectorTooltipDecorator]}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="bottom"
        args={{
            variant: 'bottom',
            size: [20, 20],
            padding: [0, 0, 0, 0],
            children: <Surface style={{ fill: '#aabbee' }} />,
        }}
        component={AxisTooltip}
        decorators={[ChartForDetectorTooltipDecorator]}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="left"
        args={{
            variant: 'left',
            size: [20, 20],
            padding: [0, 0, 0, 0],
            children: <Surface style={{ fill: '#aabbee' }} />,
        }}
        component={AxisTooltip}
        decorators={[ChartForDetectorTooltipDecorator]}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## Position and anchor

`AxisTooltip` accepts props `translate` and `anchor`.

<Canvas>
    <Story
        name="custom placement"
        args={{
            variant: 'top',
            translate: [0, 0],
            anchor: [0.5, 0.5],
            size: [20, 20],
            padding: [0, 0, 0, 0],
            children: <Surface style={{ fill: '#aabbee' }} />,
        }}
        component={AxisTooltip}
        decorators={[ChartForDetectorTooltipDecorator]}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## Other props

`AxisTooltip` shares many props with `Tooltip`. See documentation for `Tooltip` for further details.
