import { ArgsTable, Canvas, Meta, Source, Story } from '@storybook/addon-docs'
import { Chart, Legend, LegendColorScale } from '../../src'
import { ChartWithColorLegendDecorator } from './decorators'

# LegendColorScale

<Meta title="Core/Components/LegendColorScale" />
export const Template = args => <LegendColorScale {...args} />

`LegendColorScale` draws a rectangle with a color scale.

<ArgsTable of={LegendColorScale} />

## Variants

Prop `variant` determines where the tick marks appear in relation to the scale rectangle.

<Canvas>
    <Story
        name="bottom"
        args={{
            variant: 'bottom',
            position: [0, 0],
            size: [80, 12],
            horizontal: true,
        }}
        component={LegendColorScale}
        decorators={[ChartWithColorLegendDecorator]}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="top"
        args={{
            variant: 'top',
            position: [0, 24],
            size: [80, 12],
            horizontal: true,
        }}
        component={LegendColorScale}
        decorators={[ChartWithColorLegendDecorator]}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="left"
        args={{
            variant: 'left',
            position: [60, 24],
            size: [12, 100],
            horizontal: false,
        }}
        component={LegendColorScale}
        decorators={[ChartWithColorLegendDecorator]}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="right"
        args={{
            variant: 'right',
            position: [0, 24],
            size: [12, 100],
            horizontal: false,
        }}
        component={LegendColorScale}
        decorators={[ChartWithColorLegendDecorator]}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## Tuning

Various props can be used to tune the tick marks and labels.

<Canvas>
    <Story
        name="tuning ticks"
        args={{
            variant: 'bottom',
            horizontal: true,
            position: [0, 0],
            size: [80, 12],
            ticks: 3,
            tickSize: 0,
            labelOffset: 6,
            style: { strokeWidth: 1, stroke: '#222222' },
        }}
        component={LegendColorScale}
        decorators={[ChartWithColorLegendDecorator]}
    >
        {Template.bind({})}
    </Story>
</Canvas>
