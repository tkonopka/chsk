import { ViewClip } from '../../../src'

export default {
    title: 'Core/Components/Views/ViewClip',
    component: ViewClip,
}

// this is not used, but without the additional export the mdx page does not render (?)
export const Default = {
    render: () => <></>,
    name: 'default',
}
