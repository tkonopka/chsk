import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { Chart, Diamond } from '../src'
import { ChartDecorator } from './decorators'

# Diamond

<Meta title="Core/Components/Diamond" />
export const Template = args => <Diamond {...args} />

`Diamond` displays a diamond shape.

<ArgsTable of={Diamond} />

## Styles

Diamonds can be styled using css.

<Canvas>
    <Story
        name="styled"
        args={{
            style: { fill: '#dd9999', stroke: '#993333', strokeWidth: '1px' },
            cx: 160,
            cy: 110,
            r: 20,
        }}
        component={Diamond}
        decorators={[ChartDecorator]}
    >
        {Template.bind({})}
    </Story>
</Canvas>
