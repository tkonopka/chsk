import { ArgsTable, Canvas, Meta, Source, Story } from '@storybook/addon-docs'
import { Chart } from '@chask/core'
import { Bar } from '../src'

<Meta title="Addons/Annotation/Overview" />

# Annotations

Package `@chask/annotation` provides components intended for labeling datasets or data subsets. These components are relevant to several chart types.
