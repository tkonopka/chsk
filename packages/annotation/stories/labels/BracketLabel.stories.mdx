import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { Chart, Surface, View, Axis, GridLines } from '@chsk/core'
import { BracketLabel } from '../../src'
import { ChartBandViewAxisDecorator, ChartLinearViewAxisDecorator } from '../decorators'

<Meta title="Addons/Annotation/Labels/BracketLabel" />

# BracketLabel

export const Template = args => <BracketLabel {...args} />

`BracketLabel` displays a bracket and a text label. A 'bracket' is a line that forms a shape like '[' or ']'.

<ArgsTable of={BracketLabel} />

## Ticks

The feature that distinguishes `BracketLabel` from `LineLabel` is the presence of ticks at the ends of the line.
The length of these ticks is determined by prop `tickSize`.

<Canvas>
    <Story
        name="short ticks"
        args={{
            start: ['C', 108],
            end: ['F', 108],
            expansion: [0.5, 0.5],
            tickSize: 7,
            lineStyle: { fillOpacity: 0 },
            textStyle: { textAnchor: 'middle' },
            children: 'selection',
        }}
        component={BracketLabel}
        decorators={[ChartBandViewAxisDecorator]}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="long ticks"
        args={{
            start: ['C', 90],
            end: ['E', 20],
            units: ['view', 'view'],
            expansion: [0.5, 0.5],
            tickSize: 20,
            translate: [20, -20],
            lineStyle: { fillOpacity: 0 },
            textStyle: { textAnchor: 'middle' },
            children: 'selection',
        }}
        component={BracketLabel}
        decorators={[ChartBandViewAxisDecorator]}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## Other settings

`BracketLabel` shares many props and features with `LineLabel`, for example with regard to orientation of text, rotations, and styling.
See documentation for `LineLabel` for details.
