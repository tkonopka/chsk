import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { Chart } from '@chsk/core'
import { Triangle } from '../../src'
import { ChartSymbolDecorator } from './decorators'

<Meta title="Addons/Annotation/Symbols/Triangle" />

# Triangle

export const Template = args => <Triangle {...args} />

`Triangle` displays a triangle shape.

<ArgsTable of={Triangle} />

## Styles

Triangles can be styled using css.

<Canvas>
    <Story
        name="styled"
        args={{
            style: { fill: '#dd9999', stroke: '#993333', strokeWidth: '1px' },
            cx: 80,
            cy: 50,
            r: 20,
        }}
        component={Triangle}
        decorators={[ChartSymbolDecorator]}
    >
        {Template.bind({})}
    </Story>
</Canvas>
