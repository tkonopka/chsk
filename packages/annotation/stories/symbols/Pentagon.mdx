import { ArgTypes, Canvas, Meta } from '@storybook/blocks'
import * as PentagonStories from './Pentagon.stories'

<Meta of={PentagonStories} />

# Pentagon

`Pentagon` displays a five-sided polygon.

<ArgTypes of={PentagonStories} />

## Styles

Pentagons can be styled using css.

<Canvas of={PentagonStories.Styled} />
