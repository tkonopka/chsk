import { ArgsTable, Canvas, Meta, Source, Story } from '@storybook/addon-docs'
import { Chart, Axis, GridLines, Legend } from '@chask/core'
import { Bar, Bars } from '../src'
import dataGroups from './dataGroups.json'

<Meta title="Charts/Band/Examples" />

# Examples of bar charts

## Stacked charts

<Canvas>
    <Story name="stacked, with legend">
        <Chart
            id="stacked-with-legend"
            size={[400, 300]}
            padding={[40, 120, 40, 60]}
            style={{ display: 'inline-block' }}
        >
            <Bar
                data={dataGroups}
                keys={['x', 'y', 'z']}
                stacked={true}
                scaleIndex={{
                    variant: 'band',
                    domain: ['alpha', 'beta'],
                    padding: 0.2,
                }}
            >
                <GridLines variant={'y'} style={{ stroke: '#dddddd', strokeWidth: 1 }} />
                <Axis variant={'bottom'} />
                <Axis variant={'left'} />
                <Bars />
                <Legend
                    position={[240, 0]}
                    size={[60, 80]}
                    units={'absolute'}
                    anchor={[0, 0]}
                    padding={[0, 12, 0, 12]}
                    title={'Legend Title'}
                />
            </Bar>
        </Chart>
    </Story>
</Canvas>
