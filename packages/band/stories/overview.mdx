import { Meta } from '@storybook/blocks'

<Meta title="Addons/Band/Overview" />

# Charts with one discrete axis

Package `@chsk/band` provides components for charts with one discrete (band) axis and one continuous axis.
Examples include bar charts and box plots.
