import { ArgTypes, Canvas, Meta, Source } from '@storybook/blocks'
import * as DendrogramTreeStories from './DendrogramTree.stories'

# DendrogramTree

<Meta of={DendrogramTreeStories} />

`DendrogramTree` draws a dendrogram hierarchy.

<ArgTypes of={DendrogramTreeStories} />

## Styling

`DendrogramTree` draws lines to represent a tree hierarchy. Lines can be styled with css.

<Canvas of={DendrogramTreeStories.Styling} />
