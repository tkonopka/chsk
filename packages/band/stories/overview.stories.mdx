import { Meta } from '@storybook/addon-docs'

<Meta title="Addons/Band/Overview" />

# Charts with one discrete axis

Package `@chsk/band` package provides components for charts with one discrete (band) and one continuous axis. Examples include bar charts and box plots.
