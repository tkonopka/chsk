import { ArgsTable, Canvas, Meta, Source, Story } from '@storybook/addon-docs'
import { Chart } from '@chask/core'
import { Bar } from '../src'

<Meta title="Charts/Band/Overview" />

# Charts with one discrete axis

Package `@chask/band` package provides components for charts with one discrete (band) and one continuous axis. Examples include bar charts and box plots.
