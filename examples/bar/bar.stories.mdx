import { ArgsTable, Canvas, Meta, Source, Story } from '@storybook/addon-docs'
import { Controller } from '../Controller'
import {
    generateStackedData,
    StackedVerticalBandChart,
    StackedHorizontalBandChart,
} from './stacked.charts.tsx'
import {
    generateGroupedData,
    generateDivergingGroupedData,
    GroupedVerticalBandChart,
    GroupedHorizontalBandChart,
    GroupedHorizontalAutoBandChart,
} from './grouped.charts.tsx'
import { generateMultiViewsData, MultipleViewsBarChart } from './MultipleViewsBarChart'
import { generateCustomGroupsBarData, CustomGroupsBarChart } from './CustomGroupsBarChart'
import { generateBandLabelsData, BandLabelsChart } from './BandLabelsChart'
import { generateSurveyData, SurveyBarChart } from './SurveyBarChart'
import { generateArrowsData, ArrowsBarChart } from './ArrowsBarChart'

<Meta title="Gallery/Bar charts" />

# Examples of bar charts

## Grouped bars

<Canvas>
    <Story name="grouped, vertical">
        <Controller generator={generateGroupedData} chart={GroupedVerticalBandChart} />
    </Story>
</Canvas>

<Canvas>
    <Story name="grouped, horizontal">
        <Controller generator={generateGroupedData} chart={GroupedHorizontalBandChart} />
    </Story>
</Canvas>

<Canvas>
    <Story name="grouped, diverging">
        <Controller
            generator={generateDivergingGroupedData}
            chart={GroupedHorizontalAutoBandChart}
        />
    </Story>
</Canvas>

## Stacked bars

<Canvas>
    <Story name="stacked, vertical">
        <Controller generator={generateStackedData} chart={StackedVerticalBandChart} />
    </Story>
</Canvas>

<Canvas>
    <Story name="stacked, horizontal">
        <Controller generator={generateStackedData} chart={StackedHorizontalBandChart} />
    </Story>
</Canvas>

## Custom layout

<Canvas>
    <Story name="custom layout">
        <Controller
            generator={generateCustomGroupsBarData}
            chart={CustomGroupsBarChart}
            steps={['axes', 'A', 'B', 'C']}
        />
    </Story>
</Canvas>

<Canvas>
    <Story name="survey">
        <Controller
            generator={generateSurveyData}
            chart={SurveyBarChart}
            comment={
                <span>
                    The structure / layout in this example is inspired by{' '}
                    <a href="https://observablehq.com/@observablehq/plot-diverging-stacked-bar">
                        this blog post
                    </a>
                    .
                </span>
            }
        />
    </Story>
</Canvas>

<Canvas>
    <Story name="arrows">
        <Controller
            generator={generateArrowsData}
            chart={ArrowsBarChart}
            steps={['axes', 'data']}
            comment={
                <span>
                    In this 'Bar' view, a custom component draws arrows instead of rectangles.
                </span>
            }
        />
    </Story>
</Canvas>

## Multiple views

<Canvas>
    <Story name="multiple views">
        <Controller generator={generateMultiViewsData} chart={MultipleViewsBarChart} />
    </Story>
</Canvas>

## Band labels

<Canvas>
    <Story name="band labels">
        <Controller generator={generateBandLabelsData} chart={BandLabelsChart} />
    </Story>
</Canvas>
