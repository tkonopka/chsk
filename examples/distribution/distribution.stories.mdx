import { Canvas, Meta, Story } from '@storybook/addon-docs'
import { Controller } from '../Controller'
import { generateQuantileGroupsData, QuantileGroupsChart } from './QuantileGroupsChart'
import { generateStripAndBoxData, StripAndBoxChart } from './StripAndBoxChart'
import {
    generateManyDistributionsData,
    ManyDistributionsStripChart,
} from './ManyDistributionsStripChart'
import { generateWaterfallStripData, WaterfallStripChart } from './WaterfallStripChart'

<Meta title="Gallery/Distribution charts" />

# Examples of distribution charts (strips, quantiles)

<Canvas>
    <Story name="quantiles">
        <Controller generator={generateQuantileGroupsData} chart={QuantileGroupsChart} />
    </Story>
</Canvas>

<Canvas>
    <Story name="strips and quantiles">
        <Controller
            generator={generateStripAndBoxData}
            chart={StripAndBoxChart}
            steps={['axes', 'strips', 'quantiles', 'strips', 'strips']}
        />
    </Story>
</Canvas>

<Canvas>
    <Story name="many distributions">
        <Controller generator={generateManyDistributionsData} chart={ManyDistributionsStripChart} />
    </Story>
</Canvas>

<Canvas>
    <Story name="waterfall">
        <Controller generator={generateWaterfallStripData} chart={WaterfallStripChart} />
    </Story>
</Canvas>
