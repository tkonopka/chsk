import { Canvas, Meta, Story } from '@storybook/addon-docs'
import { Controller } from '../Controller'
import { generateRandomWalksData, RandomWalksChart } from './RandomWalksChart'
import { generateManyRandomWalksData, ManyRandomWalksChart } from './ManyRandomWalksChart'
import { generateRandomWalkData, RandomWalkAreaChart } from './RandomWalkAreaChart'
import { generateOverlayData, LineOverlayChart } from './LineOverlayChart'
import { generateSurvivalData, SurvivalChart } from './SurvivalChart'
import { generateTimeSeriesData, TimeSeriesChart } from './TimeSeriesChart'
import { generateInnerPanelLineData, InnerPanelLineChart } from './InnerPanelLineChart'
import {
    generateTimeQuarterlySeriesData,
    TimeQuarterlySeriesChart,
} from './TimeQuarterlySeriesChart'
import { generateForecastData, ForecastChart } from './ForecastChart'

<Meta title="Gallery/Line charts" />

# Examples of line charts

<Canvas>
    <Story name="two lines">
        <Controller generator={generateRandomWalksData} chart={RandomWalksChart} />
    </Story>
</Canvas>

<Canvas>
    <Story name="many lines">
        <Controller generator={generateManyRandomWalksData} chart={ManyRandomWalksChart} />
    </Story>
</Canvas>

<Canvas>
    <Story name="survival">
        <Controller
            generator={generateSurvivalData}
            chart={SurvivalChart}
            steps={['control', 'treated']}
        />
    </Story>
</Canvas>

<Canvas>
    <Story name="time series">
        <Controller generator={generateTimeSeriesData} chart={TimeSeriesChart} />
    </Story>
</Canvas>

<Canvas>
    <Story name="custom tick marks">
        <Controller generator={generateTimeQuarterlySeriesData} chart={TimeQuarterlySeriesChart} />
    </Story>
</Canvas>

<Canvas>
    <Story name="inset">
        <Controller
            generator={generateInnerPanelLineData}
            chart={InnerPanelLineChart}
            steps={['axes', 'data', 'box', 'inner']}
        />
    </Story>
</Canvas>

<Canvas>
    <Story name="forecast">
        <Controller generator={generateForecastData} chart={ForecastChart} />
    </Story>
</Canvas>

<Canvas>
    <Story name="area gradient">
        <Controller generator={generateRandomWalkData} chart={RandomWalkAreaChart} />
    </Story>
</Canvas>

<Canvas>
    <Story name="line overlays">
        <Controller generator={generateOverlayData} chart={LineOverlayChart} />
    </Story>
</Canvas>
