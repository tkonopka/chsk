import { Canvas, Meta, Story } from '@storybook/addon-docs'
import { Controller } from '../Controller'
import { LineHistogramChart } from './LineHistogramChart'
import { MultipleViewsHistogramChart } from './MultipleViewsHistogramChart'

<Meta title="Gallery/Histograms" />

# Examples of histograms

## Histograms

<Canvas>
    <Story name="annotations">
        <Controller
            chart={LineHistogramChart}
            steps={['axes', 'data', 'primary-label', 'secondary-label']}
        />
    </Story>
</Canvas>

## Multiple views

<Canvas>
    <Story name="multiple views">
        <Controller
            chart={MultipleViewsHistogramChart}
            steps={['subtitle', 'small', 'medium', 'large']}
        />
    </Story>
</Canvas>
